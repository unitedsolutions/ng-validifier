export default class  {
    private settings;
    private asyncs;
    private $el;
    private $elContainer;
    private pendingClass;
    constructor(settings: any, control: any);
    private display(type, on);
    private classOperator(operation);
    container(on: any): void;
    pending(on: any): void;
    success(on: any): void;
}
